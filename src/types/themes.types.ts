export interface AppTheme {
  colors: {
    blue: string,
    orange: string,
    yellow: string,
    green: string,
    white: string,
    red: string,
  }
}

export type Color = 'blue' | 'yellow' | 'red' | 'orange' | 'green' | 'white'
