import React from 'react'
import { SwitchAuthProps } from '../../../types/auth.types'

const SwitchAuth = ({authType, changeType}: SwitchAuthProps) => {
  return <div>
    
  </div>
}

export default SwitchAuth
